import { React } from 'react';

class UserDetail extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        let user = props.user;

        return (
            <div></div>
        );
    }
}

export default UserDetail;
